currying is a function - that doesn't take all of its arguments up front,
instead  each function takes an argument and then returns another function with another argument

the last function will be the last called function with our expected result

